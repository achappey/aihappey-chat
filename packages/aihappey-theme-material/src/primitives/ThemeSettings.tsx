import { Typography } from "@mui/material";

export const ThemeSettings = () => <Typography variant="body2" color="text.secondary">Material theme settings are provided by Material ThemeProvider props.</Typography>;

